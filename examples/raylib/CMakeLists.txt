find_package(raylib QUIET)
if(raylib_FOUND)
    add_executable(teliqos_raylib_demo main.cpp)
    target_link_libraries(teliqos_raylib_demo PRIVATE teliqos raylib)
else()
    message(STATUS "raylib not found, skipping raylib example")
endif()
